[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Clean URL for the demo form → monday.com handler
[[redirects]]
  from = "/api/demo"
  to = "/.netlify/functions/demo"
  status = 200
  force = true
